# Pixera Caddyfile — auto-HTTPS via Let's Encrypt
# Set API_DOMAIN in your .env file (e.g. pixera-demo.duckdns.org)
# Only the API needs to be public. MinIO and Keycloak are internal only.

{$API_DOMAIN} {
    reverse_proxy api:8080
}
